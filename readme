open terminal and enter this command to download tailwind
npm install tailwindcss @tailwindcss/vite
Run the following command to create a virtual environment and install the required libraries:
python -m venv venv
source venv/bin/activate  # On Windows, use `venv\Scripts\activate`
pip install flask flask-cors
Run the Flask Application:
python app.py
